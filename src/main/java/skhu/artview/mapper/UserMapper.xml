<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
             "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="skhu.artview.mapper.UserMapper">

    <select id="findOne" resultType="User">
    SELECT * FROM user WHERE id = #{id}
  </select>
  
  <select id="findOneByLoginId" resultType="User">
  	SELECT * FROM user WHERE login_id = #{login_id}
  </select>
  
  <select id="findAll" resultType="User">
    SELECT  *
    FROM user 
  </select>
  
  <select id="findAges" resultType="Age">
    SELECT  *
    FROM age
  </select>
  
  <select id="findCity" resultType="City">
    SELECT  *
    FROM city
  </select>
  
  <select id="findDistrict" resultType="District">
    SELECT  d.*
    FROM district d
    INNER JOIN city c ON d.city_id = c.id
    WHERE d.city_id = #{c_id};
  </select>
  
  <select id="findArtfield" resultType="artfield">
    SELECT  *
    FROM artfield
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT user (fav_artfield_id, login_id, pwd, name, phone, email, age)
    VALUES (#{fav_artfield_id}, #{login_id}, #{pwd}, #{name}, #{phone}, #{email}, #{age})
  </insert>

	<insert id="saveNormal" useGeneratedKeys="true" keyProperty="id">
    INSERT user (login_id, pwd, name, phone, email,age)
    VALUES (#{login_id}, #{pwd}, #{name}, #{phone}, "",#{age})
  </insert>

  <delete id="delete">
    DELETE FROM User WHERE id = #{id}
  </delete>
  
	<select id="findMyApplicant" resultType="User">
		SELECT * FROM user u
		LEFT JOIN p_apply p
		ON u.id = p.userId
		WHERE p.state = 0 AND projectId = #{projectId}
	</select>
	
	<select id="findMyMember" resultType="User">
		SELECT * FROM user u
		LEFT JOIN p_apply p
		ON u.id = p.userId
		WHERE p.state = 1 AND projectId = #{projectId}
	</select>
	
	<select id="findByEmail"  resultType="User">
		SELECT * FROM User WHERE email = #{email}
	</select>
	
	<select id="findByEmailAndName"  resultType="User">
		SELECT * FROM User WHERE email = #{email} and name = #{name}
	</select>

	

	<update id="updatePassword">
		UPDATE User SET
		password = #{password}
		WHERE email = #{email} 
	</update>
	<update id="updateEnabled">
		UPDATE User SET
		enabled=1
		WHERE email = #{email} 
	</update>

</mapper>

